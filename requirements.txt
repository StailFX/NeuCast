fastapi>=0.100.0
uvicorn>=0.23.0
uvloop>=0.19.0
httptools>=0.6.0
jinja2>=3.1.0
sqlalchemy>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.18
tensorflow>=2.13.0
scikit-learn>=1.3.0
python-multipart>=0.0.6
catboost>=1.2
xgboost>=2.0.0
lightgbm>=4.0.0
PyWavelets>=1.4.0
h5py>=3.9.0
fpdf2>=2.7.0
psycopg2-binary>=2.9.0
celery>=5.3.0
redis>=5.0.0
transformers>=4.30.0
torch>=2.0.0
shap>=0.43.0
websockets>=12.0
# Async Postgres for the high-frequency ingest pipeline (app/highfreq).
# Lightweight (~5MB), used only by the highfreq L2 consumer container.
asyncpg>=0.29.0
# Optional: Foundation models для time-series (Tier 4, 5.2, 5.3 opt-in checkbox).
# При отсутствии любой из библиотек foundation.py делает graceful fallback.
# Если установлены несколько — run_prediction ансамблирует их прогнозы (ниже variance).
# ВНИМАНИЕ: веса Moirai (uni2ts) под CC-BY-NC-4.0 (non-commercial) — по дефолту
# отключён в FOUNDATION_MODELS, включается явно: FOUNDATION_MODELS="chronos,timesfm,moirai".
# uni2ts тянет heavy deps (~1GB): gluonts, lightning, jax, torch (CPU). Опционален.
chronos-forecasting>=1.4.0
timesfm>=1.3.0
uni2ts>=1.2.0
# Tier 5.4: Locally-adaptive calibration через NGBoost (opt-in, LOCAL_CALIBRATION=1).
# Модель оценивает σ(x) → локальный inflation-коэффициент для conformal bands.
# Graceful fallback (ratio=1.0) при отсутствии либы.
ngboost>=0.5.0
